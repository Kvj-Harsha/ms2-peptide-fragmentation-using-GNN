# Core numerical / ML stack
# PyTorch: install the CPU or CUDA build separately for reproducibility, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# The version is pinned loosely so the wheel index can pick the right build.
torch>=2.2,<2.6
torch-geometric>=2.5,<2.7

numpy>=1.26,<2.2
scipy>=1.11
pandas>=2.1
tqdm>=4.66
pyyaml>=6.0

# Dataset access
datasets>=2.18
huggingface-hub>=0.23

# Demo app (optional, only needed for app/streamlit_app.py)
streamlit>=1.33
matplotlib>=3.8
plotly>=5.20

# Optional LLM narration for the demo (only needed if GROQ_API_KEY is set)
# groq>=0.9

# Dev / test
pytest>=8.0
